package validate

// JSON schema validation in v0.1 is represented by typed manifest checks plus
// JSON/YAML parse checks. The public schemas are kept under schemas/ for tools
// that want draft-07 validation outside the CLI.
